import { Meta } from '@storybook/blocks';

<Meta title="Welcome" />

# Welcome to Lyfeguard Design System

<div style={{
  display: 'grid',
  gap: '24px',
  padding: '48px',
  background: 'linear-gradient(135deg, rgba(23,123,154,0.08), rgba(0,255,178,0.08))',
  border: '1px solid var(--color-theme-border)',
  borderRadius: '20px',
  boxShadow: '0 4px 24px rgba(23,123,154,0.12)',
  marginBottom: '48px',
}}>
  <div>
    <p style={{
      margin: 0,
      marginBottom: '16px',
      color: 'var(--color-brand-500)',
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      fontWeight: 600,
      fontSize: '12px',
      fontFamily: "'Epilogue', sans-serif",
    }}>
      Build with clarity and confidence
    </p>
    <h2 style={{
      margin: 0,
      marginBottom: '16px',
      fontSize: '36px',
      fontWeight: 700,
      color: 'var(--color-theme-text-primary)',
      lineHeight: 1.2,
      fontFamily: "'Epilogue', sans-serif",
    }}>
      Explore the UI toolkit, tokens, and patterns that power Lyfeguard products.
    </h2>
    <p style={{
      margin: 0,
      color: 'var(--color-theme-text-secondary)',
      fontSize: '18px',
      lineHeight: 1.6,
      maxWidth: '780px',
    }}>
      Storybook is your sandbox for trying components, verifying accessibility, and sharing implementation-ready examples with the team.
    </p>
  </div>
  <div style={{ display: 'flex', gap: '16px', flexWrap: 'wrap' }}>
    <a
      href="?path=/docs/foundations-accessibility--docs"
      style={{
        display: 'inline-flex',
        alignItems: 'center',
        gap: '8px',
        padding: '12px 24px',
        background: 'var(--color-theme-surface)',
        border: '1.5px solid var(--color-theme-border)',
        borderRadius: '8px',
        color: 'var(--color-theme-text-primary)',
        textDecoration: 'none',
        fontWeight: 600,
        fontSize: '16px',
        transition: 'all 0.2s ease',
        fontFamily: "'Epilogue', sans-serif",
      }}
      onMouseEnter={(e) => {
        e.currentTarget.style.background = 'var(--color-theme-surfaceHover)';
        e.currentTarget.style.borderColor = 'var(--color-theme-borderHover)';
        e.currentTarget.style.transform = 'translateY(-2px)';
        e.currentTarget.style.boxShadow = '0 4px 12px rgba(23,123,154,0.15)';
      }}
      onMouseLeave={(e) => {
        e.currentTarget.style.background = 'var(--color-theme-surface)';
        e.currentTarget.style.borderColor = 'var(--color-theme-border)';
        e.currentTarget.style.transform = 'translateY(0)';
        e.currentTarget.style.boxShadow = 'none';
      }}
    >
      Accessibility essentials →
    </a>
    <a
      href="?path=/docs/components-button--docs"
      style={{
        display: 'inline-flex',
        alignItems: 'center',
        gap: '8px',
        padding: '12px 24px',
        background: 'var(--color-brand-500)',
        border: '1.5px solid var(--color-brand-500)',
        borderRadius: '8px',
        color: 'var(--color-neutral-0)',
        textDecoration: 'none',
        fontWeight: 600,
        fontSize: '16px',
        transition: 'all 0.2s ease',
        fontFamily: "'Epilogue', sans-serif",
      }}
      onMouseEnter={(e) => {
        e.currentTarget.style.background = 'var(--color-brand-800)';
        e.currentTarget.style.borderColor = 'var(--color-brand-800)';
        e.currentTarget.style.transform = 'translateY(-2px)';
        e.currentTarget.style.boxShadow = '0 4px 12px rgba(23,123,154,0.3)';
      }}
      onMouseLeave={(e) => {
        e.currentTarget.style.background = 'var(--color-brand-500)';
        e.currentTarget.style.borderColor = 'var(--color-brand-500)';
        e.currentTarget.style.transform = 'translateY(0)';
        e.currentTarget.style.boxShadow = 'none';
      }}
    >
      Try a component →
    </a>
  </div>
</div>

## How Storybook is organised

<div style={{ 
  display: 'grid', 
  gap: '24px', 
  gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
  marginBottom: '48px',
}}>
  <div style={{
    padding: '24px',
    borderRadius: '12px',
    background: 'var(--color-theme-surface)',
    border: '1px solid var(--color-theme-border)',
    transition: 'all 0.2s ease',
    boxShadow: '0 2px 8px rgba(0,0,0,0.04)',
  }}
  onMouseEnter={(e) => {
    e.currentTarget.style.borderColor = 'var(--color-brand-500)';
    e.currentTarget.style.boxShadow = '0 4px 16px rgba(23,123,154,0.12)';
    e.currentTarget.style.transform = 'translateY(-2px)';
  }}
  onMouseLeave={(e) => {
    e.currentTarget.style.borderColor = 'var(--color-theme-border)';
    e.currentTarget.style.boxShadow = '0 2px 8px rgba(0,0,0,0.04)';
    e.currentTarget.style.transform = 'translateY(0)';
  }}
  >
    <h3 style={{ 
      marginTop: 0, 
      marginBottom: '12px',
      fontFamily: "'Epilogue', sans-serif",
      fontSize: '22px',
      fontWeight: 600,
      color: 'var(--color-theme-text-primary)',
    }}>Foundations</h3>
    <p style={{ 
      margin: 0, 
      color: 'var(--color-theme-text-secondary)',
      lineHeight: 1.6,
      fontSize: '15px',
    }}>
      Guidelines for colour, typography, spacing, and accessibility. Start here to understand the system's visual language.
    </p>
  </div>
  <div style={{
    padding: '24px',
    borderRadius: '12px',
    background: 'var(--color-theme-surface)',
    border: '1px solid var(--color-theme-border)',
    transition: 'all 0.2s ease',
    boxShadow: '0 2px 8px rgba(0,0,0,0.04)',
  }}
  onMouseEnter={(e) => {
    e.currentTarget.style.borderColor = 'var(--color-brand-500)';
    e.currentTarget.style.boxShadow = '0 4px 16px rgba(23,123,154,0.12)';
    e.currentTarget.style.transform = 'translateY(-2px)';
  }}
  onMouseLeave={(e) => {
    e.currentTarget.style.borderColor = 'var(--color-theme-border)';
    e.currentTarget.style.boxShadow = '0 2px 8px rgba(0,0,0,0.04)';
    e.currentTarget.style.transform = 'translateY(0)';
  }}
  >
    <h3 style={{ 
      marginTop: 0, 
      marginBottom: '12px',
      fontFamily: "'Epilogue', sans-serif",
      fontSize: '22px',
      fontWeight: 600,
      color: 'var(--color-theme-text-primary)',
    }}>Components</h3>
    <p style={{ 
      margin: 0, 
      color: 'var(--color-theme-text-secondary)',
      lineHeight: 1.6,
      fontSize: '15px',
    }}>
      Interactive stories for every UI building block. Each story includes controls, props, and accessibility expectations.
    </p>
  </div>
  <div style={{
    padding: '24px',
    borderRadius: '12px',
    background: 'var(--color-theme-surface)',
    border: '1px solid var(--color-theme-border)',
    transition: 'all 0.2s ease',
    boxShadow: '0 2px 8px rgba(0,0,0,0.04)',
  }}
  onMouseEnter={(e) => {
    e.currentTarget.style.borderColor = 'var(--color-brand-500)';
    e.currentTarget.style.boxShadow = '0 4px 16px rgba(23,123,154,0.12)';
    e.currentTarget.style.transform = 'translateY(-2px)';
  }}
  onMouseLeave={(e) => {
    e.currentTarget.style.borderColor = 'var(--color-theme-border)';
    e.currentTarget.style.boxShadow = '0 2px 8px rgba(0,0,0,0.04)';
    e.currentTarget.style.transform = 'translateY(0)';
  }}
  >
    <h3 style={{ 
      marginTop: 0, 
      marginBottom: '12px',
      fontFamily: "'Epilogue', sans-serif",
      fontSize: '22px',
      fontWeight: 600,
      color: 'var(--color-theme-text-primary)',
    }}>Layout</h3>
    <p style={{ 
      margin: 0, 
      color: 'var(--color-theme-text-secondary)',
      lineHeight: 1.6,
      fontSize: '15px',
    }}>
      Guidance for stacking, grids, and responsive behaviour so screens stay consistent across breakpoints.
    </p>
  </div>
  <div style={{
    padding: '24px',
    borderRadius: '12px',
    background: 'var(--color-theme-surface)',
    border: '1px solid var(--color-theme-border)',
    transition: 'all 0.2s ease',
    boxShadow: '0 2px 8px rgba(0,0,0,0.04)',
  }}
  onMouseEnter={(e) => {
    e.currentTarget.style.borderColor = 'var(--color-brand-500)';
    e.currentTarget.style.boxShadow = '0 4px 16px rgba(23,123,154,0.12)';
    e.currentTarget.style.transform = 'translateY(-2px)';
  }}
  onMouseLeave={(e) => {
    e.currentTarget.style.borderColor = 'var(--color-theme-border)';
    e.currentTarget.style.boxShadow = '0 2px 8px rgba(0,0,0,0.04)';
    e.currentTarget.style.transform = 'translateY(0)';
  }}
  >
    <h3 style={{ 
      marginTop: 0, 
      marginBottom: '12px',
      fontFamily: "'Epilogue', sans-serif",
      fontSize: '22px',
      fontWeight: 600,
      color: 'var(--color-theme-text-primary)',
    }}>Technical Patterns</h3>
    <p style={{ 
      margin: 0, 
      color: 'var(--color-theme-text-secondary)',
      lineHeight: 1.6,
      fontSize: '15px',
    }}>
      Implementation notes for themes, tokens, composition, and responsive props to keep builds predictable.
    </p>
  </div>
</div>

## Quick links

<div style={{ 
  display: 'grid', 
  gap: '20px', 
  gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
  marginBottom: '48px',
}}>
  <a href="?path=/docs/foundations-colours--docs" style={{
    textDecoration: 'none',
    color: 'inherit',
    display: 'block',
  }}>
    <div style={{
      padding: '24px',
      borderRadius: '12px',
      border: '1px solid var(--color-theme-border)',
      background: 'var(--color-theme-surface)',
      display: 'grid',
      gap: '12px',
      transition: 'all 0.2s ease',
      boxShadow: '0 2px 8px rgba(0,0,0,0.04)',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.borderColor = 'var(--color-brand-500)';
      e.currentTarget.style.boxShadow = '0 4px 16px rgba(23,123,154,0.12)';
      e.currentTarget.style.transform = 'translateY(-2px)';
      e.currentTarget.style.background = 'var(--color-theme-surfaceHover)';
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.borderColor = 'var(--color-theme-border)';
      e.currentTarget.style.boxShadow = '0 2px 8px rgba(0,0,0,0.04)';
      e.currentTarget.style.transform = 'translateY(0)';
      e.currentTarget.style.background = 'var(--color-theme-surface)';
    }}
    >
      <p style={{ 
        margin: 0, 
        color: 'var(--color-brand-500)', 
        fontSize: '12px',
        fontWeight: 600,
        letterSpacing: '0.05em',
        textTransform: 'uppercase',
        fontFamily: "'Epilogue', sans-serif",
      }}>Foundations</p>
      <h4 style={{ 
        margin: 0,
        fontFamily: "'Epilogue', sans-serif",
        fontSize: '20px',
        fontWeight: 600,
        color: 'var(--color-theme-text-primary)',
      }}>Colour tokens</h4>
      <p style={{ 
        margin: 0, 
        color: 'var(--color-theme-text-secondary)',
        lineHeight: 1.6,
        fontSize: '15px',
      }}>
        Explore the palette, contrast guidance, and status colours used across the product.
      </p>
    </div>
  </a>
  <a href="?path=/docs/foundations-typography--docs" style={{
    textDecoration: 'none',
    color: 'inherit',
    display: 'block',
  }}>
    <div style={{
      padding: '24px',
      borderRadius: '12px',
      border: '1px solid var(--color-theme-border)',
      background: 'var(--color-theme-surface)',
      display: 'grid',
      gap: '12px',
      transition: 'all 0.2s ease',
      boxShadow: '0 2px 8px rgba(0,0,0,0.04)',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.borderColor = 'var(--color-brand-500)';
      e.currentTarget.style.boxShadow = '0 4px 16px rgba(23,123,154,0.12)';
      e.currentTarget.style.transform = 'translateY(-2px)';
      e.currentTarget.style.background = 'var(--color-theme-surfaceHover)';
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.borderColor = 'var(--color-theme-border)';
      e.currentTarget.style.boxShadow = '0 2px 8px rgba(0,0,0,0.04)';
      e.currentTarget.style.transform = 'translateY(0)';
      e.currentTarget.style.background = 'var(--color-theme-surface)';
    }}
    >
      <p style={{ 
        margin: 0, 
        color: 'var(--color-brand-500)', 
        fontSize: '12px',
        fontWeight: 600,
        letterSpacing: '0.05em',
        textTransform: 'uppercase',
        fontFamily: "'Epilogue', sans-serif",
      }}>Foundations</p>
      <h4 style={{ 
        margin: 0,
        fontFamily: "'Epilogue', sans-serif",
        fontSize: '20px',
        fontWeight: 600,
        color: 'var(--color-theme-text-primary)',
      }}>Typography scale</h4>
      <p style={{ 
        margin: 0, 
        color: 'var(--color-theme-text-secondary)',
        lineHeight: 1.6,
        fontSize: '15px',
      }}>
        Reference headings, body styles, and the font stacks that keep copy readable.
      </p>
    </div>
  </a>
  <a href="?path=/docs/layout-overview--docs" style={{
    textDecoration: 'none',
    color: 'inherit',
    display: 'block',
  }}>
    <div style={{
      padding: '24px',
      borderRadius: '12px',
      border: '1px solid var(--color-theme-border)',
      background: 'var(--color-theme-surface)',
      display: 'grid',
      gap: '12px',
      transition: 'all 0.2s ease',
      boxShadow: '0 2px 8px rgba(0,0,0,0.04)',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.borderColor = 'var(--color-brand-500)';
      e.currentTarget.style.boxShadow = '0 4px 16px rgba(23,123,154,0.12)';
      e.currentTarget.style.transform = 'translateY(-2px)';
      e.currentTarget.style.background = 'var(--color-theme-surfaceHover)';
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.borderColor = 'var(--color-theme-border)';
      e.currentTarget.style.boxShadow = '0 2px 8px rgba(0,0,0,0.04)';
      e.currentTarget.style.transform = 'translateY(0)';
      e.currentTarget.style.background = 'var(--color-theme-surface)';
    }}
    >
      <p style={{ 
        margin: 0, 
        color: 'var(--color-brand-500)', 
        fontSize: '12px',
        fontWeight: 600,
        letterSpacing: '0.05em',
        textTransform: 'uppercase',
        fontFamily: "'Epilogue', sans-serif",
      }}>Layout</p>
      <h4 style={{ 
        margin: 0,
        fontFamily: "'Epilogue', sans-serif",
        fontSize: '20px',
        fontWeight: 600,
        color: 'var(--color-theme-text-primary)',
      }}>Responsive patterns</h4>
      <p style={{ 
        margin: 0, 
        color: 'var(--color-theme-text-secondary)',
        lineHeight: 1.6,
        fontSize: '15px',
      }}>
        Learn how container, stack, and grid primitives handle spacing across breakpoints.
      </p>
    </div>
  </a>
  <a href="?path=/docs/technical-patterns-overview--docs" style={{
    textDecoration: 'none',
    color: 'inherit',
    display: 'block',
  }}>
    <div style={{
      padding: '24px',
      borderRadius: '12px',
      border: '1px solid var(--color-theme-border)',
      background: 'var(--color-theme-surface)',
      display: 'grid',
      gap: '12px',
      transition: 'all 0.2s ease',
      boxShadow: '0 2px 8px rgba(0,0,0,0.04)',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.borderColor = 'var(--color-brand-500)';
      e.currentTarget.style.boxShadow = '0 4px 16px rgba(23,123,154,0.12)';
      e.currentTarget.style.transform = 'translateY(-2px)';
      e.currentTarget.style.background = 'var(--color-theme-surfaceHover)';
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.borderColor = 'var(--color-theme-border)';
      e.currentTarget.style.boxShadow = '0 2px 8px rgba(0,0,0,0.04)';
      e.currentTarget.style.transform = 'translateY(0)';
      e.currentTarget.style.background = 'var(--color-theme-surface)';
    }}
    >
      <p style={{ 
        margin: 0, 
        color: 'var(--color-brand-500)', 
        fontSize: '12px',
        fontWeight: 600,
        letterSpacing: '0.05em',
        textTransform: 'uppercase',
        fontFamily: "'Epilogue', sans-serif",
      }}>Technical patterns</p>
      <h4 style={{ 
        margin: 0,
        fontFamily: "'Epilogue', sans-serif",
        fontSize: '20px',
        fontWeight: 600,
        color: 'var(--color-theme-text-primary)',
      }}>Token-first builds</h4>
      <p style={{ 
        margin: 0, 
        color: 'var(--color-theme-text-secondary)',
        lineHeight: 1.6,
        fontSize: '15px',
      }}>
        Keep implementations aligned with our token and theming strategy.
      </p>
    </div>
  </a>
</div>

## Tips for a better experience

<div style={{
  padding: '24px',
  borderRadius: '12px',
  background: 'var(--color-theme-surface)',
  border: '1px solid var(--color-theme-border)',
  marginTop: '32px',
}}>
  <ul style={{
    margin: 0,
    paddingLeft: '24px',
    color: 'var(--color-theme-text-secondary)',
    lineHeight: 1.8,
    fontSize: '16px',
  }}>
    <li>Use the <strong style={{ color: 'var(--color-theme-text-primary)' }}>Theme</strong> toolbar control to preview light and dark modes instantly.</li>
    <li>Switch preview <strong style={{ color: 'var(--color-theme-text-primary)' }}>Backgrounds</strong> to test components on canvas, surface, or brand-heavy backdrops.</li>
    <li>Explore the <strong style={{ color: 'var(--color-theme-text-primary)' }}>Docs</strong> tab on each story for usage notes, props tables, and accessibility considerations.</li>
    <li>Keep examples token-driven—avoid hard-coding colours or spacing in your own stories.</li>
  </ul>
</div>
